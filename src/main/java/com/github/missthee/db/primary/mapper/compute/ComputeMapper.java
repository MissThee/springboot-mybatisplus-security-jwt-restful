package server.db.primary.mapper.compute;

import org.springframework.stereotype.Component;
import server.config.tkmapper.common.CommonMapper;
import server.db.primary.model.compute.Compute;

@Component
public interface ComputeMapper extends CommonMapper<Compute> {
}